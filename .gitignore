# ===== Google logging feature — kept local, not pushed to GitHub =====
gsheet_logger.py
GOOGLE_SHEETS_SETUP.md

# ===== Secrets / credentials (NEVER commit these) =====
.streamlit/secrets.toml
*service-account*.json
service_account.json
gcp*.json
credentials.json
client_secret*.json

# ===== User-requested exclusions =====
dataset/
sample/
uploads/
data_downloader.py
embedding.pkl
filenames.pkl

# ===== MLflow / model artifacts (large or regenerable) =====
mlruns/
mlartifacts/
mlflow.db
models/v1_baseline.h5
models/*_curves.png
# NOTE: models/v2_transfer.h5 (14 MB) + models/labels.json + *_metrics.json ARE
# committed so the API image and Kubernetes deployment work out of the box.
# v1_baseline.h5 is 296 MB (over GitHub's 100 MB limit); regenerate via training.

# ===== Python / environment =====
.venv/
venv/
env/
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# ===== IDE / tooling =====
.idea/
.vscode/
.claude/

# ===== Docker helper / build artifacts (local only) =====
docker_bootstrap.ps1
bootstrap.log
build.out.log
build.err.log
build.pid

# ===== OS =====
Thumbs.db
.DS_Store
