# FastAPI serving dependencies (Python 3.7).
# Installed ON TOP of the base requirements.txt (which already pins tensorflow,
# keras-vggface, mtcnn, opencv, numpy, scikit-learn, Pillow used by the API).
#
#   pip install -r requirements.txt -r requirements-api.txt
#
# Versions below are the last ones supporting Python 3.7.
fastapi==0.83.0
uvicorn==0.20.0
python-multipart==0.0.5
